'use client';

import { SearchFilter, FilterContainer } from './filters/filter-components';

export function AdminTagsFilters() {
  return (
    <FilterContainer>
      <SearchFilter />
    </FilterContainer>
  );
}
